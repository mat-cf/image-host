server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    location /upload {
        proxy_pass http://api:8080;
    }

    location /uploads/ {
        proxy_pass http://api:8080;
    }
}